// The merchant dashboard has one interface language. Content locales remain selectable
// so merchants can inspect the generated translations without switching the admin UI.
export const adminText = (_en: string, zh: string, _es: string) => zh
